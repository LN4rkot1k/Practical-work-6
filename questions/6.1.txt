""" ÷иклы for используютс€ дл€ прохождени€ по заданной последовательности. 
Ќа каждой итерации переменной, определенной в цикле for, будет присваиватьс€ следующее значение в списке 
""" 

# ‘ункци€ range (n) возвращает список [0, 1, 2, 3, ..., n-1] 

for i in range(5):    	# дл€ каждого числа i в диапазоне 0-4 
    print(i)         	# эта строка выполн€етс€ 5 раз. ѕервый раз i равно 0, затем 1, затем 2, затем 3, затем 4 
  

primes = [2, 3, 5, 7]   # создание нового списка 

# ¬ыведите на экран каждый элемент из primes, использу€ цикл for по i

for ?? in ???
    print(primes[i]) 

# ѕеребор элементов списка возможен и без использовани€ индекса элемента
for prime in primes 
    print(prime) 

  

#--------------------------------------# 

  

""" —троки очень похожи на списки в Python. ¬ы можете использовать другую строку, чтобы перебрать ее. 
""" 

hello_world = "Hello, World!" 

for ch in hello_world:    # печать каждого символа строки 
    print(ch) 
  


length = 0     		# переменна€ length 

# »спользуйте цикл, чтобы подсчитать, сколько символов содержит hello_world. —охраните это число в переменной length 

for ... in hello_world: 
    length += 1    	# увеличить переменную на 1 при каждом проходе цикла 
  

print(len(hello_world) == length) 